interface ICredential {
    id: number;       // ID numérico de las credenciales
    username: string; // nombre de usuario
    password: string; // contraseña del usuario
}

export default ICredential;
